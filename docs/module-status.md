# Ход разработки: модули × V-модель

**Стадия:** 📋 план → 📐 проект → 🛠 в работе → 🚀 прод · **Готовность:** ⬜ → 🟡 → ✅
«║» — разделители после Ст (статус ┃ описание) и после Код (описание ┃ тестирование).

Дата среза: 2026-07-31. Источник оценок: `grace lint` (0 ошибок), **190 зелёных тестов** (unit + integration + e2e; 6 из них - на живой модели), полный демо-цикл на враждебной базе с подключённой LLM, **11 проверок верификатора**, критерии приёмки §7 проектного документа. Предыдущие срезы: 103 теста до разбора 4, 155 до разбора 5, 163 до внешнего ревью, 177 до подключения модели.

| Раздел | Модуль | Ст | ║ | Потр | Треб | Арх | Кнтр | Код | ║ | Unit | Интг | Сист | Вриф | Влд |
|---|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| Ядро замен | M-MAPPER | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | ✅ | ⬜ |
| Ядро замен | M-LLM | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | ✅ | ⬜ |
| Ядро замен | M-CORPUS | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | 🟡 | ⬜ |
| --- | --- | - | ║ | - | - | - | - | - | ║ | - | - | - | - | - |
| Планирование | M-PROFILER | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | 🟡 | ✅ | ✅ | ✅ | ⬜ |
| Планирование | M-CLASSIFIER | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | ✅ | ⬜ |
| Планирование | M-FK-CLOSURE | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | ✅ | ⬜ |
| Планирование | M-POLICY | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | ✅ | ⬜ |
| Планирование | M-PLAN-GRAPH | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | 🟡 | ✅ | ✅ | ✅ | ⬜ |
| --- | --- | - | ║ | - | - | - | - | - | ║ | - | - | - | - | - |
| Исполнение | M-EXECUTOR | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | ✅ | ⬜ |
| Исполнение | M-POSTPROC | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | 🟡 | ⬜ |
| Исполнение | M-RUNLOG | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | ✅ | ⬜ |
| --- | --- | - | ║ | - | - | - | - | - | ║ | - | - | - | - | - |
| Проверка | M-VERIFIER | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | ✅ | ⬜ |
| --- | --- | - | ║ | - | - | - | - | - | ║ | - | - | - | - | - |
| Поставка | M-CLI | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | ✅ | ⬜ |
| Поставка | M-REPORT | 🚀 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ⬜ | 🟡 | 🟡 | 🟡 | 🟡 |
| Поставка | M-DEMO-DB | 🛠 | ║ | ✅ | ✅ | ✅ | ✅ | ✅ | ║ | ✅ | ✅ | ✅ | ✅ | ⬜ |

## Что изменилось после разбора 4

| Модуль | Что закрыто | Чем доказано |
|---|---|---|
| M-CLASSIFIER | Признак ключа больше не отменяет оба голоса | `test_leak_paths.py`: `PRIMARY KEY (email)` даёт `email`, а не `keep` |
| M-PROFILER · Unit 🟡→ | Ложный первичный ключ из неполного join | `test_profiler.py`: FK-тёзка PK во временной схеме |
| M-POLICY | Ключ-ПДн, ключи jsonb в отпечатке, монотонность классов, пороги из params | 9 + 12 тестов; демо-план перестроен |
| M-PLAN-GRAPH · Unit ⬜→🟡 | Белый список override вынесен в `apply_overrides` и покрыт юнитом | `test_leak_paths.py`, 4 теста |
| M-MAPPER | Инъективность отделена от псевдослучайности | `test_data_corruption.py`: 2200 исходников → 2200 замен |
| M-EXECUTOR | Перемешивание по сущности; соль передаётся явно | Корреляция «оклад ↔ начисления» 0.005 → **0.9997** |
| M-POSTPROC | Round-trip экранирования; `length_policy` исполняется | 6 форм round-trip; проба целостности в верификаторе |
| M-VERIFIER | Схема, ключи и места проб — из каталога и манифеста | Конвейер собран на схеме `crm` |
| M-RUNLOG | Гейт публикации стал механизмом | `publish` без verify не создаёт даже каталог |
| M-DEMO-DB | Враждебные случаи по каждой находке | 28 ИНН юрлиц, 101 табельный вне окна, 183 NULL, 91 слэш |

## Что изменилось после разбора 5

| Модуль | Что закрыто | Чем доказано |
|---|---|---|
| M-POSTPROC | Идентификатор в тексте опознаётся контрольной суммой, а не пунктуацией | Было 783 СНИЛС в копии (374 совпадали с исходными) — стало **0** |
| M-VERIFIER | Сверка всех идентификаторов источника с копией; порог деградаций; пропущенная проверка блокирует | 11 проверок; проверка не наследует слепоту прохода 2 |
| M-VERIFIER | Разнообразие сравнивается с достижимым пределом корпуса | Отчёт называет потолок: `first_name 6.31`, `last_name 7.75` |
| M-POLICY | Первичные ключи и пороги — в плане; валидация отвергает нереализованную разметку | Тест на таблице с ключом `pay_no` |
| M-EXECUTOR | Исчерпание карты 1:1 — ошибка, а не вечный цикл | Воспроизведено на корпусе из двух значений |
| M-MAPPER | Однозначное число больше не заменяется само на себя | Перестановка десяти цифр от соли |
| M-CLI | Соль без умолчания и без подсказки в тексте ошибки | `compose` требует переменную, `Makefile` задаёт демо-значение |

## Что изменилось после внешнего ревью (`docs/code-review-2026-07-31.md`)

| Модуль | Что закрыто | Чем доказано |
|---|---|---|
| M-POSTPROC | **Блокер:** двойная трансформация ломала сквозную консистентность | Дефисная запись давала `96205129297`, плоская — `83377091728`; теперь все написания дают одно значение |
| M-POSTPROC | Проход 2 переписан на один проход с защитой занятых участков | Порядко-зависимость устранена структурно, а не порядком вызовов |
| M-POSTPROC | Страховочный слой: телефон без «+», СНИЛС через пробелы, кириллические домены, карты по Луну | 14 тестов в `test_code_review_fixes.py` |
| M-CLI | `run` проверяет план и отпечаток схемы перед исполнением | §3.5 обещал остановку — теперь она есть и на исполнении |
| M-POLICY | Белые списки значений: стратегия, `llm_mode`, семантический тип | Опечатка `fkae` больше не проходит валидацию |
| M-EXECUTOR | Неизвестная стратегия и нереализованный `generalize` роняют прогон | Молчаливый пропуск был fail-open через весь конвейер |
| M-POLICY | Email не понижается в `fake` и не уезжает в корпус организаций | `ivan.petrov@mail.ru` больше не становится «ооо вектор» |
| M-MAPPER | Целое с ведущим нулём — явный отказ вместо ложной инъективности | Коллизия воспроизводилась 1 на 6000 смешанных входов |

## Что дало подключение модели

Отклонение «LLM отсутствует физически» снято: модуль `M-LLM` даёт четыре роли за одним интерфейсом, поставщик выбирается окружением, отсутствие поставщика не ошибка.

| Роль | Что делает | Видит ПДн | Замер на демо |
|---|---|:--:|---|
| 1. Голос классификатора | метаданные колонок → семантический тип | нет | 69 колонок, 51 с типом; 7/8 на путающих случаях |
| 2. Корпуса | порождает материал замен с нуля | нет | проходит `validate_corpus` |
| 3. Карта 1:1 | осмысленные замены названий | не-ПДн | инъективна, пробелы достраивает генератор |
| 4. Третий эшелон NER | вердикт по спорному фрагменту | **да** → только в контуре | деградации **88 → 0** |

Граница безопасности в коде: внешний поставщик на роли 4 получает отказ исключением. Проверено тестом.

## Чем обоснованы неполные оценки

| Ячейка | Почему не ✅ |
|---|---|
| M-CORPUS · Вриф | Критерий §7 «правдоподобность» (слепая выборка: ревьюер не отличает копию от оригинала лучше случайного) требует живого человека — автоматизировать нечем |
| M-PROFILER · Unit | Юнитом покрыт `addr_parse_ratio`; интроспекция каталога проверяется интеграционно против живой БД — для неё это осмысленнее моков |
| M-PLAN-GRAPH · Unit | Юнитом покрыт только `apply_overrides`; узлы графа — тонкие обёртки, вся логика в модулях под ними, сам граф проверен интеграционно (гейт, resume, подтверждение, дифф) |
| M-POSTPROC · Вриф | Целевой recall NER ≥0.85 на целевом домене не измерен: замерена только доля неразобранных фрагментов (88 → 0 при подключённой модели), а это не recall |
| M-REPORT · Unit/Интг/Сист | Тестов нет; страница проверена вручную в браузере на живом стенде + автопроверкой на отсутствие канареек в секции after. Демо-специфика оставлена сознательно (NonGoal изменения) |
| M-REPORT · Влд 🟡 | Единственное, что реально развёрнуто и доступно внешнему проверяющему (за basic-auth) |
| Влд (столбец) | Никто ещё не разрабатывал и не тестировал **на** санитизированной копии — главная ценность не подтверждена |

## Измеренные критерии приёмки §7

| Критерий | Порог | Факт |
|---|---|---|
| Полнота классификации | recall ≥0.98, precision ≥0.85 | **recall 1.000, precision 0.958** на 23 размеченных из 69 колонок |
| Канарейки | K/K в источнике, 0/K в копии | **11/11** и 0/11 на обоих входах |
| **Идентификаторы источника** | ни одного значения ИНН/СНИЛС/ОГРН источника в копии | 0 (сверка по контрольным суммам, не по канарейкам) |
| **Деградации прохода 2** | не выше объявленного планом порога | 88 ≤ 120, порог прошёл гейт |
| Отсутствие исходных значений | построчно `fake(x) ≠ x` | чисто по всем `direct`/`fake`/`generate` |
| Ссылочная целостность | все ограничения восстановлены | 30/30 |
| Объём | `count(*)` совпадает | 14 таблиц |
| Разнообразие | просадка энтропии ≤10% **от достижимого** | в пределах порога; потолок корпуса назван в отчёте для четырёх колонок |
| Сквозная консистентность | одна сущность → одна замена везде | таблица + склейка + свободный текст |
| **Целостность не-ПДн** | текст, не являющийся ПДн, доезжает дословно | новая проверка; поймала бы порчу экранированием |
| **Сохранение связей** | связанные денежные колонки не расходятся | корреляция 1.000 → 0.9997 |
| **Валидность идентификаторов** | КС ИНН/СНИЛС/ОГРН верны в копии | 61/61 ИНН контрагентов валидны, включая 10-значные |
| **Уникальность под UNIQUE** | нет коллизий замен | 2001 табельный → 2001 уникальный |
| Воспроизводимость | чек-суммы двух прогонов совпадают | подтверждено e2e |
| Несвязываемость копий | разные получатели → разные псевдонимы | подтверждено e2e |
| Вход «дамп» | тот же конвейер от файла | 8/8 критериев |
| Верификатор умеет падать | подсаженная утечка ловится | 3 негативных теста |
| **Работа на чужой схеме** | конвейер собирается вне `hr` | план и артефакты на схеме `crm` |

**Не измерены:** производительность обоих проходов (демо-масштаб мал), доля ручного разбора ≤15%, recall NER, правдоподобность.

## User Stories

| UC | История | Стадия | Валидация | Что изменилось |
|---|---|:--:|:--:|---|
| UC-1 | Инженер получает обезличенную копию прод-базы и разворачивает её в dev | 🛠 | 🟡 | Появилась команда публикации; конвейер собирается вне схемы `hr` |
| UC-2 | Разработчик работает на копии: приложение стартует, валидации проходят | 🛠 | 🟡 | ИНН юрлиц валидны, табельные уникальны, NULL остаётся NULL, переполнение колонки ловится до restore |
| UC-3 | Аналитик считает статистику на копии и получает те же распределения | 🛠 | 🟡 | Связь «оклад ↔ начисления» сохранена (0.9997), мультимножества совпадают |
| UC-4 | Офицер ИБ ревьюит план санитизации и аппрувит | 🛠 | 🟡 | Подтверждение читается на гейте, несёт автора и не подделывается конфигом; путь черновика верен |
| UC-5 | Подрядчику выдаётся копия, несвязываемая с копией другого подрядчика | 🛠 | ✅ | Соль обязательна, отпечаток сверяется между проходами |
| UC-6 | Оператор принимает на вход готовый дамп, а не подключение | 🛠 | ✅ | — |
| UC-7 | Проверяющий открывает стенд и видит сопоставление «до/после» | 🚀 | 🟡 | — |
| UC-8 | Юрист подтверждает соответствие 152-ФЗ по материалам инструмента | 📐 | ⬜ | Отклонения (LLM, Presidio) записаны явно; отчёта о соответствии по-прежнему нет |

UC-1..UC-4 🟡: механизмы есть и проверены тестами, живого сценария с человеком не было.
UC-5, UC-6 ✅: подтверждены e2e-тестом и отдельным прогоном из дампа.

## Главный разрыв

**Левая ветвь закрыта полностью, правая — до «Вриф» включительно; провал на «Влд».** Все 15 модулей имеют код, контракты и место в графе `.grace`; 190 тестов зелёные; сквозной прогон проходит на обоих входах ТЗ и на посторонней схеме; критериев §7 измерено на пять больше. Но валидация не начиналась: никто не разворачивал копию и не работал на ней.

Самый дешёвый шаг закрыть валидацию: **развернуть санитизированный дамп и прогнать на нём приложение, читающее эти таблицы.** Если ORM стартует, валидации ИНН/СНИЛС проходят и отчёты считаются — UC-2 и UC-3 закрываются за один прогон. Это и есть переход от «сделали по ТЗ» к «решили задачу»: платят за верификацию, ценность — в валидации.

## Что вскрыли ревизии

**Разбор 5 (результат правок), 2026-07-31.** Уровнем выше разбора 4: **проверки проверяли ровно те форматы, которые умеет обработчик.** Канареечный СНИЛС написан с дефисами, база пишет подряд — 783 значения уехали в копию при девяти зелёных проверках, 374 из них дословно совпадали с исходными. Правило на будущее: проверка не должна опознавать объект тем же признаком, что и проверяемый механизм. Обработчик смотрит на формат — проверка обязана считать контрольную сумму. Обработчик знает список канареек — проверка обязана сравнивать множества целиком. Проверка, которую нельзя провести, обязана блокировать, а не молчать. Полностью: `docs/design-review-5.md`.

**Разбор 4 (реализация), 2026-07-31.** Главный вывод был не про отдельные дефекты, а про сам тестовый набор: **103 зелёных теста проверяли код на данных, специально подобранных так, чтобы он работал.** В демо-базе не было ни одного 10-значного ИНН, ни одного табельного вне окна, ни одного обратного слэша, ни одной связи между зарплатой и начислениями. Ни одна из подтверждённых находок не была покрыта тестом. Отсюда порядок работы: сначала враждебные данные, потом правка, потом тест, который на старом коде красный.

Четыре пути утечки были невидимы для всех восьми проверок **одновременно** — по ним утечка и произошла бы на незнакомой базе.

**Предыдущая ревизия (V-модель), 2026-07-30.** Пять тестовых файлов, объявленных планом, не существовали; финальный гейт GRACE это пропустил, потому что прогоняет целевую команду, а не пофайловые команды задач. `os.environb` есть только на POSIX. Одноимённые модули тестов ломали совместный сбор pytest. Верификатор ни разу не проверялся на способность падать.
